Comportement désiré :

T = 2 sec
If not net
  If cache present : return cache + listen
  Else return fail + listen
Else, Check freshness for T
  If timeout : like when if not net
  If fresh : show cache
  If not fresh : fetch
    If timeout : return cache + listen
    Else : show


Donc :
Il me faut une abstraction qui me permet de :
Stream des résultats.
